type Client {
  id: ID!
  phone: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  # issuedNumber: [IssuedNumber!]!
}

type PendingTurnInfo {
  shopId: ID!
  shopName: String
  turnInfo: TurnInfo
}

type Query {
  myTurns: [PendingTurnInfo!]!
}

type Mutation {
  cancelTurn(turnId: ID!): Boolean!
  requestTurn(shopId: String!): IssuedNumber!
}
