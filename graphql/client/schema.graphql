type Client {
  id: ID!
  phone: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  # issuedNumber: [IssuedNumber!]!
}

type Query {
  client(id: ID): Client
}

type Mutation {
  signUp(client: ClientSignupInput!): Client!    
}

input ClientSignupInput {
  phone: String!
}
