overwrite: true
schema: ./graphql/**/*.graphql
documents: null
generates:
  ./graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-operations"
    config:
      useIndexSignature: true