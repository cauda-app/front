overwrite: true
schema: ./pages_/api/graphql/**/*.graphql
documents: null
generates:
  ./graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true